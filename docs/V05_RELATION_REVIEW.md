# v0.5 relation review summary

Nova v1 exposes 97 unique lexical resonance pairs. These have now all been semantically reviewed.

## Decision coverage

- 92 lexical pairs produce one or more accepted semantic edges.
- 5 lexical pairs are explicitly rejected as semantic edges.
- 96 accepted typed edges exist because two source pairs legitimately carry multiple relations.

## Edge provenance

- 56 edges are `source_explicit`.
- 40 edges are `source_supported` formalizations.
- 0 accepted Nova semantic edges are currently `proposed`.

Lux/Phosphenic hypotheses therefore remain outside the Nova graph instead of being smuggled into source-derived structure.

## Files

- `data/v0.5/typed-edges-reviewed-pass1.json` — first 35 accepted edges.
- `data/v0.5/typed-edges-reviewed-pass2.json` — remaining 61 accepted edges.
- `data/v0.5/resonance-no-semantic-edge.json` — five explicit no-edge decisions.
- `audit/v0.5-typed-relations-complete.md` — aggregate audit.
- `audit/v0.5-graph-topology-preexport.md` — topology before generated export.

## Main structural observation

The reviewed network naturally concentrates around two different kinds of hubs:

- `Lucidité` — cognitive/monitoring hub;
- `Zone de bascule` — temporal/transition-context hub.

This is an emergent property of the reviewed source-derived network, not a manually imposed hierarchy.
